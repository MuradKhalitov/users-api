version: '3.8'

services:
  user-api:
    container_name: user-api
    restart: always
    image: user-api
    ports:
      - "8080:8080"
    networks:
      - my-network

networks:
  my-network:
    external: true